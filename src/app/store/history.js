import { createBrowserHistory } from 'history' // We are importing a method that will create an object that REACT will know what it IS and what it WAS in the past

export const history = createBrowserHistory(); // This will export a HISTORY instance that all other files can use